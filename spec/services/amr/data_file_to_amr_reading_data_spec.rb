require 'rails_helper'

describe Amr::DataFileToAmrReadingData do
  context 'for a typical config' do
  end

  context 'for a row per reading data file with a single datetime column' do
  end

  context 'for a row per reading data file that splits the date and time into two columns' do
  end
end
