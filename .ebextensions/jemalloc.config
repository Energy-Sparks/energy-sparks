option_settings:
  aws:elasticbeanstalk:application:environment:
    LD_PRELOAD: /usr/lib64/libjemalloc.so.2

packages:
  yum:
    # already installed because depended on by bind-utils but feels a good idea to be explicit
    jemalloc: []
