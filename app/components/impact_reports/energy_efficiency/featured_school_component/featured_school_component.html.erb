<%= tag.div id: id, class: classes do %>
  <%= render Layout::GridComponent.new(
        id: 'energy-efficiency-featured',
        cols: 2,
        theme: :light,
        classes: 'rounded-3 p-4 my-4',
        cell_classes: 'my-auto'
      ) do |grid| %>
    <%= grid.with_feature_card(size: :md) do |feature| %>
      <%= feature.with_header title: impact_t('energy_efficiency.featured.title') %>
      <%= feature.with_description do
            description
          end %>
      <%= feature.with_button(
            t('common.view_dashboard'),
            school_path(school),
            style: :secondary
          ) %>
    <% end %>
    <%= grid.with_image(src: image) %>
  <% end %>
<% end %>
