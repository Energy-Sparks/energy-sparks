<div class="compact-timeline-component">
  <% observations.each do |observation| %>
    <%= render TimelineComponent::Observation.new(observation: observation, show_actions: show_actions, compact: true) %>
  <% end %>
</div>
