<%= tag.div id: id, class: classes do %>
  <% cells.each_slice(cols) do |cell_group| %>
    <div class="row">
      <% cell_group.each do |cell| %>
          <%= cell %>
      <% end %>
    </div>
  <% end %>
<% end %>
