<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <% @bs5 = params[:bs5] # Sneaky way to make bs5 switching possible using preview params (see carousel preview) %>
    <%= render 'shared/page/head' %>
  </head>
  <body vocab="http://schema.org/">
    <div class='p-2'>
      <%= yield %>
    </div>
  </body>
</html>
