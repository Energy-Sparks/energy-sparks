<nav class="navbar navbar-fixed-top">
  <%= link_to "EnergySparks", root_path, class: "navbar-brand" %>
  <ul class="nav navbar-nav">
    <li class="nav-item active">
      <%= link_to "Home", root_url %>
    </li>
    <li class="nav-item">
      <%= link_to "Participating Schools", schools_path %>
    </li>
    <% if user_signed_in? && current_user.manages_school? %>
      <li class="nav-item">
        <%= link_to "Your School", school_path(current_user.school) %>
      </li>
    <% end %>
    <% if user_signed_in? && current_user.admin? %>
      <li class="nav-item">
        <%= link_to "Users", users_index_path %>
      </li>
    <% end %>
  </ul>
  <div class="pull-xs-right">
    <% if user_signed_in? %>
      Logged in as <%= current_user.email %>
      [<%= link_to "Sign Out", destroy_user_session_path, method: :delete %>]
    <% else %>
      <%= link_to "Sign In", new_user_session_path %>
    <% end %>
  </div>
</nav>
