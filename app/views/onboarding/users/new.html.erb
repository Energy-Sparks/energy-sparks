<h4><%= t('onboarding.users.new.title') %></h4>

<%= simple_form_for @user, url: onboarding_users_path(@school_onboarding, @user), remote: true do |f| %>
  <%= render 'form', f: f, user: @user, school_onboarding: @school_onboarding %>
  <button class="btn" data-dismiss="modal" aria-hidden="true"><%= t('common.labels.cancel') %></button>
  <%= f.submit t('onboarding.users.new.create_account'), class: 'btn btn-rounded'%>
<% end %>
