<%= f.input :name, required: false %>
<%= f.input :email, required: true %>

<%= f.input :role, label: "Type", required: true, collection: User.roles.except(:guest, :admin, :school_onboarding, :pupil, :group_admin, :analytics, :volunteer).keys, label_method: :titleize, hint: 'Staff accounts have access to all teach and pupil functionality. School administrators can manage users and school details' %>

<%= f.input :staff_role_id, label: 'Role', required: true, collection: StaffRole.order(:title), label_method: :title, hint: 'What is the users relationship with the school?'%>
