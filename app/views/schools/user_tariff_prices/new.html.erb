<h1>Add tariff rate</h1>

<%= simple_form_for :user_tariff_price, url: school_user_tariff_user_tariff_prices_path(@school, @user_tariff) do |f| %>

  <%= render 'form', user_tariff_price: @user_tariff_price, f: f %>

<% end %>
