<% content_for :page_title, t('interventions.edit.page_title') %>
<% content_for :main_classes, 'bg-blue-pale' %>
<%= render Tasks::Header.new(recording: @observation) %>

<%= render 'form', observation: @observation %>
