<% content_for :page_title, 'Update an annual usage estimate' %>

<div class="d-flex justify-content-between align-items-center">
  <h1>Update your estimated annual energy consumption</h1>
  <div>
    <%= link_to_help_for_feature :annual_usage_estimate, css: "btn btn-outline-dark rounded-pill font-weight-bold" %>
  </div>
</div>

<div class="row">
  <div class="col-md-8">
    <p>
      When we have limited information about your historical energy consumption we are unable to
      provide you with a detailed progress report that will help you <%= link_to 'achieve your energy saving
      targets', school_school_targets_path(@school) %>.
    </p>
    <p>
      If you can provide us with an estimate of your annual energy consumption, then we can use this to calculate
      your monthly targets and progress.
    </p>
  </div>
</div>

<div class="row">
  <div class="col-md-8">
    <h3>Your estimate</h3>

    <div class="alert alert-info">
      <p>
        <%= far_icon("lightbulb fa-lg") %>
        <strong>Tip</strong>: Check our energy bill.<br>
        Your recent energy bills should provide the data you need.
      </p>
    </div>

    <%= render "form", estimated_annual_consumption: @estimated_annual_consumption, school: @school %>
  </div>
</div>

<% if current_user.admin? %>
  <div class="row mt-4">
    <div class="alert alert-danger col-md-8">
      <strong>Admin only:</strong>
      <%= link_to 'Delete', school_estimated_annual_consumption_path(@school, @estimated_annual_consumption), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
    </div>
  </div>
<% end %>
