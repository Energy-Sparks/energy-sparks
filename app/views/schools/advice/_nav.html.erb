<%= render(PageNavComponent.new(name:'Advice Home', icon: 'home', href: school_advice_path(@school), options: { match_controller: true })) do |c| %>
  <% c.with_section do |s| %>
    <% s.with_item(name: 'Total energy use', href: '#') %>
  <% end %>
  <% c.with_section(name: "Electricity", icon: 'bolt', bgcolor: '#93e1f6', visible: @school.has_electricity?) do |s| %>
    <% s.with_item(name: 'Baseload', href: school_advice_baseload_path(@school)) %>
    <% s.with_item(name: 'Another long link', href: school_advice_path(@school)) %>
  <% end %>
  <% c.with_section(name: "Gas", icon: 'fire', bgcolor: '#FF8438', visible: @school.has_gas?) do |s| %>
  <% end %>
  <% c.with_section(name: "Storage heaters", icon: 'window-maximize', bgcolor: '#20c997', visible: @school.has_storage_heaters?) do |s| %>
  <% end %>
  <% c.with_section(name: "Solar", icon: 'sun', bgcolor: '#ffc107', visible: @school.has_solar_pv?) do |s| %>
  <% end %>
  <% c.with_section(name: "Carbon", icon: 'cloud-download-alt', bgcolor: '#e6e6e6') do |s| %>
  <% end %>
<% end %>
