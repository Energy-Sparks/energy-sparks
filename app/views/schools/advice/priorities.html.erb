<% if Flipper.enabled?(:new_dashboards_2024, current_user) %>
  <% content_for :page_title, "#{t('advice_pages.index.priorities.title')} | #{@school.name}" %>

  <% content_for :dashboard_header do %>
    <%= render DashboardHeaderComponent.new(
          title: 'advice_pages.index.priorities.title',
          intro: nil,
          show_school: false,
          school: @school
        ) %>
  <% end %>

  <div class="container">
    <div class="row mt-2">
      <div class="col-md-3 col-lg-3 col-xl-2 advice-page-nav">
        <%= render 'schools/advice/nav', school: @school, advice_pages: @advice_pages %>
      </div>
      <div class="col-md-9 col-lg-9 col-xl-10">
        <h2>
          <%= t('advice_pages.index.priorities.table_title') %>
        </h2>

        <p>
          <%= t('advice_pages.index.priorities.intro') %>
        </p>

        <p>
          <%= t('advice_pages.index.priorities.table_note') %>
        </p>

        <%= render 'schools/advice/index/priority_table', management_priorities: @management_priorities, school: @school %>
      </div>
    </div>
  </div>
<% else %>
  <%= render 'schools/advice/index/advice_index_page', school: @school, advice_pages: @advice_pages, tab: @tab,
                                                       priority_count: @priority_count, alert_count: @alert_count  do %>
  <div class="row mt-4">
    <div class="col">
      <h2><%= t('advice_pages.index.priorities.title') %></h2>

      <p>
        <%= t('advice_pages.index.priorities.intro') %>
      </p>

      <p>
        <%= t('advice_pages.index.priorities.table_note') %>
      </p>

      <%= render 'schools/advice/index/priority_table', management_priorities: @management_priorities, school: @school %>
    </div>
  </div>
  <% end %>
<% end %>
