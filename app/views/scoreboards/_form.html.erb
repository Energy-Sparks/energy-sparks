<%= simple_form_for(scoreboard) do |f| %>

  <%= f.input :calendar_area_id, as: :select, collection: CalendarArea.where(parent_id: nil), include_blank: false, hint: 'Used for scoreboard academic years'%>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
