<% content_for :page_title, t('activities.new.page_title') %>

<% content_for :main_classes, 'bg-yellow-very-pale' %>
<%= render Tasks::Header.new(recording: @activity) %>

<%= render 'form', activity: @activity %>
