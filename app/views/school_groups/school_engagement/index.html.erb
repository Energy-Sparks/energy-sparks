<%= render 'admin/shared/dashboard_message', messageable: @school_group %>

<% content_for :header_title, "#{@school_group.name} - #{t('school_groups.school_engagement.title')}" %>

<%= t('school_groups.school_engagement.intro_html') %>
<p><%= link_to 'Download as CSV', school_group_school_engagement_index_path(@school_group, format: :csv), class: 'btn' %></p>
<%= render 'admin/reports/column_table', columns: @columns, rows: @rows %>
