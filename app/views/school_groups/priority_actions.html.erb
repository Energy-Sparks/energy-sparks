<%= render 'enhanced_header' %>

<%= render 'school_groups/advice/priority_table',
           school_group: @school_group,
           path: [:priority_actions, @school_group],
           priority_actions: @priority_actions,
           total_savings: @total_savings %>
