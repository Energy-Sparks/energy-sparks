<%= form_for(school_group) do |f| %>

  <% if school_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(school_group.errors.count, "error") %> prohibited this school_group from being saved:</h2>

      <ul>
      <% school_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :scoreboard_id, 'Scoreboard' %>
    <%= f.select :scoreboard_id, options_from_collection_for_select(Scoreboard.order(:name), 'id', 'name', (@school_group.scoreboard_id)), {include_blank: true}, { class: 'form-control' } %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
