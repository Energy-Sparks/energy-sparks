  <%= link_to 'All runs', school_group_impact_runs_path(@school_group), class: 'btn btn-secondary' %>
  <%= link_to 'Latest run data', latest_school_group_impact_runs_path(@school_group), class: 'btn btn-secondary' %>

  <%= link_to 'All reports', admin_impact_reports_path, class: 'btn btn-secondary' %>
  <%= link_to 'View report', school_group_impact_index_path(@school_group), class: 'btn btn-primary' %>
