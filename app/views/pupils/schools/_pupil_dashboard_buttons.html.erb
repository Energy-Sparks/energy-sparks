<% if show_admin_page_switch?(school) %>
  <% if params[:no_data] %>
    <%= link_to t('pupils.schools.show.admin_view'), pupils_school_path(school), class: 'btn btn-warning' %>
  <% else %>
    <%= link_to t('pupils.schools.show.user_view'), pupils_school_path(school, no_data: true), class: 'btn btn-warning' %>
  <% end %>
<% end %>
