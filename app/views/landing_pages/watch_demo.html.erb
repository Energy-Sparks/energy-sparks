<% content_for :page_title, t('campaigns.watch_demo.title') %>
<div class="container-fluid adult pt-2 pb-3">
  <div class="container">
    <h1><%= t('campaigns.watch_demo.title') %></h1>
    <p><%= t('campaigns.watch_demo.intro') %></p>
  </div>
</div>

<div class="container my-4">
  <div class="row">
    <div class="col-md-6">
      <%= render 'form', source: :demo %>
    </div>
    <div class="col-md-6 d-flex align-items-center">
      <%= image_tag('on-screen.jpg', class: 'img-fluid rounded-xl') %>
    </div>
  </div>
</div>
