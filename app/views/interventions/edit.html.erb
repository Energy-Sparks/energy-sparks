<div class="row justify-content-md-center mt-2">
  <div class="col col-lg-8">
    <h3>Update your energy saving action</h3>
    <%= simple_form_for([@observation], url: intervention_path(@observation), html: {class: 'wizard intervention'}) do |f| %>
      <%= render 'form', f: f, observation: @observation, intervention_type: @intervention_type %>
    <% end %>
  </div>
</div>
