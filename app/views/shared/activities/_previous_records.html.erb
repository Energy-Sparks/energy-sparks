<%= render PromptComponent.new(icon: 'circle-check', status: :none, classes: 'mb-4') do |prompt| %>
  <%= t('activities.previous_records.previously_completed', label: label) %>
  <strong>
    <%= link_to human_counts(list).to_s, school_timeline_path(school) %>
  </strong>
  <% if list.count > 1 %>
    <%= t('activities.previous_records.completed_most_recently_on') %>
  <% else %>
    <%= t('activities.previous_records.completed_on') %>
  <% end %>
  <strong>
    <%= link_to nice_dates(most_recent).to_s, path %>
  </strong>
<% end %>
