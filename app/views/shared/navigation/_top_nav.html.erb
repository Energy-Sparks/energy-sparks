<nav class="navbar <%= navbar_expand_class %> navbar-top">
  <div class="container">
    <%= navigation_image_link %>
    <%= render 'shared/navigation/top_nav/on_test' %>

    <div class='<%= order_expand_class %>'>
      <%= render 'shared/navigation/top_nav/locale_switcher' %>

      <!-- Toggler icon - shown on collapse -->
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav-top" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>

    <div class="navbar-collapse collapse justify-content-end" id="nav-top">
      <ul class="navbar-nav nowrap">
        <!-- Pupil activities link -->
        <li class="nav-item">
          <%= link_to t('common_nav_bar_menus.activities'), activity_categories_path, class: 'nav-link' %>
        </li>

        <!-- Adult actions link -->
        <li class="nav-item">
          <%= link_to t('common_nav_bar_menus.actions'), intervention_type_groups_path, class: 'nav-link' %>
        </li>

        <!-- Our schools menu -->
        <%= render 'shared/navigation/top_nav/our_schools_menu' %>

        <!-- Our services menu -->
        <%= render 'shared/navigation/top_nav/our_services_menu' %>

        <!-- About menu -->
        <%= render 'shared/navigation/top_nav/about_menu' %>

        <!-- Support us link -->
        <li class="nav-item">
          <%= link_to t('support_us.title'), support_us_path, class: 'nav-link' %>
        </li>

        <% if can?(:manage, :admin_functions) %>
          <!-- Manage menu - admin only -->
          <%= render 'shared/navigation/top_nav/manage_menu' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
