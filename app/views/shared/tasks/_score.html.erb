<% if task.score %>
  <span
    class="badge badge-success"
    data-toggle="tooltip"
    data-placement="bottom"
    title="<%= t("tasks.score.#{task.public_type}") %>">
    <%= task.score %>
  </span>
<% end %>
