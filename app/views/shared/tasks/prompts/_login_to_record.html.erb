<%# i18n-tasks-use t('tasks.activity.sign_in_html') %>
<%# i18n-tasks-use t('tasks.action.sign_in_html') %>
<%# i18n-tasks-use t('tasks.activity.record_this') %>
<%# i18n-tasks-use t('tasks.action.record_this') %>

<%= render PromptComponent.new(icon: 'tasks', classes: 'mb-4 theme theme-dark') do |prompt| %>
  <% prompt.with_title { t('tasks.login_to_record.are_you_an_energy_sparks_user') } %>
  <% prompt.with_content_action do |c| %>
      <% c.with_body do %>
        <%= t("tasks.#{label}.sign_in_html", count: score) %>
      <% end %>
      <% c.with_action do %>
        <%= link_to t("tasks.#{label}.record_this"),
                    sign_in_and_redirect_path(url: return_path), class: 'btn btn-primary text-nowrap' %>
      <% end %>
  <% end %>
<% end %>
