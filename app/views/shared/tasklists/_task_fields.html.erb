<div class="nested-fields">
    <div class="row p-2">
      <div class="col-md-5">
        <div class="row">
          <div class="col-1">
            <%= render IconComponent.new(name: 'up-down-left-right', classes: 'handle') %>
          </div>
          <div class="col-11">
            <%= f.select(
                  :task_source_id,
                  grouped_options_for_select(
                    task_sources,
                    f.object.task_source_id
                  ),
                  { include_blank: true },
                  { class: 'form-control select2' }
                ) %>
            <%= f.hidden_field :task_source_type, value: task_source_type %>
            <%= f.hidden_field :id %>
            <%= f.text_field :position, class: 'position' %>
            <%= f.error :task_source, id: 'task_source_error' %>
          </div>
        </div>
      </div>
      <div class="col-md-5">
        <%= f.text_area :notes, label: false, class: 'form-control select2' %>
      </div>
      <div class="col-md-2">
        <%= link_to_remove_association t('common.labels.remove'), f, class: 'btn' %>
      </div>
    </div>
</div>
