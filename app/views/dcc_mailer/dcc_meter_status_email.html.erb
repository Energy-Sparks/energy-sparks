<h1>New smart meters found</h1>

<p>The following meters were found by the DCC Checker</p>

<table style="width: 100%; margin: 20px">
  <thead>
  <tr>
    <th>School</th>
    <th>Meter</th>
    <th>Type</th>
    <th>DCC Type</th>
    <th>Checked at</th>
  </tr>
  </thead>
  <tbody>
  <% @meters.each do |meter| %>
    <tr>
      <td><%= link_to meter.school.name, school_meters_url(meter.school) %></td>
      <td><%= link_to meter.mpan_mprn, school_meter_url(meter.school, meter) %></td>
      <td><%= meter.meter_type %></td>
      <td><%= meter.dcc_meter %></td>
      <td><%= meter.dcc_checked_at %></td>
    </tr>
  <% end %>
  </tbody>
</table>
