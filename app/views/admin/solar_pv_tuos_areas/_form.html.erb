<%= simple_form_for([:admin, solar_pv_area]) do |f| %>
  <%= f.input :title, as: :string %>
  <%= f.input :description %>
  <%= f.input :latitude %>
  <%= f.input :longitude %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
