<%= render 'filter', field: :meter_type,
                     options: options_for_select(Meter.meter_types.to_a, params[:meter_type]),
                     blank: 'Any Meter Type' %>
<%= render 'admin_meter_status_filter' %>
