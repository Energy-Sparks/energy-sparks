<%- headers = ['queue_name', 'job_class', 'job_id', 'performed_at', 'finished_at', 'time_to_completion_in_seconds'] -%>
<%= CSV.generate_line headers %>
<%- @jobs.each do |job| -%>
<%= CSV.generate_line(job.values_at(*headers)) -%>
<%- end -%>
