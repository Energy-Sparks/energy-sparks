<%= component 'notice', status: @content.colour, classes: 'mb-4' do |c| %>
  <%= component 'info_bar',
      title: @content.send(params[:field]),
      icon: fa_icon(alert_icon(@alert, 'fa-3x')),
      buttons: {}
  %>
<% end %>
