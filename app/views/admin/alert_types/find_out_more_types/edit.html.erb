<%= render 'admin/alert_types/nav', alert_type: @alert_type %>

<h1>Edit Find Out More Type</h1>

<%= simple_form_for @find_out_more_type, url: admin_alert_type_find_out_more_type_path(@alert_type, @find_out_more_type) do |form| %>
  <%= render 'form', form: form, content: @content %>
  <%= form.button :submit, 'Update Find Out More type' %>
<% end %>

