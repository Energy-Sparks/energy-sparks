<%= render 'header', title: "View #{@issue.issueable ? @issue.issueable.name : ''} #{@issue.issue_type.capitalize}", issue: @issue, issueable: @issue.issueable %>

<%= render 'issue', issue: @issue, index: false %>
