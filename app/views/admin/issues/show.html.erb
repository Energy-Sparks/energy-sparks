<%= render 'header', title: "View #{@issue.issueable ? @issue.issueable.name : ''} #{@issue.issue_type.capitalize}" do %>
  <% if @issue.issueable %>
    <%= header_nav_link "Issues & notes for #{@issue.issueable.model_name.to_s.downcase}", url_for([:admin, @issue.issueable, :issues]) %>
  <% else %>
    <%= header_nav_link 'All issues & notes', admin_issues_url %>
  <% end %>
<% end %>

<%= render 'issue', issue: @issue, index: false, issueable: nil %>
