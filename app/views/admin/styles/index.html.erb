<h2>Styles</h2>

<h3>Buttons</h1>

<%= link_to 'unstyled', '/', class: 'btn' %>
<%= link_to 'btn-default', '/', class: 'btn btn-default' %>
<%= button_tag 'button', class: 'btn btn-success' %>

<hr>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <%= link_to key.to_s, '/', class: "btn btn-#{key}" %>
<% end %>
<hr>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <%= link_to "#{key} sm", '/', class: "btn btn-#{key} btn-sm" %>
<% end %>

<hr>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <%= link_to "#{key} lg", '/', class: "btn btn-lg btn-#{key}" %>
<% end %>

<hr>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <%= link_to key.to_s, '/', class: "btn btn-#{key} disabled", disabled: true %>
<% end %>

<div class='bg-grey-pale rounded p-2 my-2'>
  <h4>Outline</h4>
  <% Colours::PALETTE[:bootstrap].each do |key, _value| %>
    <%= link_to key.to_s, '/', class: "btn btn-outline-#{key}" %>
  <% end %>
  <hr>
  <% Colours::PALETTE[:bootstrap].each do |key, _value| %>
    <%= link_to key.to_s, '/', class: "btn btn-outline-#{key} disabled", disabled: true %>
  <% end %>

  <h4>Outline - transparent</h4>
    <% Colours::PALETTE[:bootstrap].each do |key, _value| %>
      <%= link_to key.to_s, '/', class: "btn btn-outline-#{key} transparent" %>
    <% end %>
  <hr>
  <% Colours::PALETTE[:bootstrap].each do |key, _value| %>
    <%= link_to key.to_s, '/', class: "btn btn-outline-#{key} transparent disabled", disabled: true %>
  <% end %>
</div>

<h4>btn-group</h4>
<span class='btn-group'>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <%= link_to key.to_s, '/', class: "btn btn-#{key}" %>
<% end %>
</span>
<hr>
<span class='btn-group'>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <%= link_to key.to_s, '/', class: "btn btn-outline-#{key}" %>
<% end %>
</span>

<h4>btn-group panel</h4>
<span class='btn-group panel'>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <%= link_to key.to_s, '/', class: "btn btn-#{key}" %>
<% end %>
</span>
<hr>
<span class='btn-group panel'>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <%= link_to key.to_s, '/', class: "btn btn-outline-#{key}" %>
<% end %>
</span>

<h3>Badges</h1>

<h4>Bootstrap 4 syntax</h4>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <% classes = "badge badge-#{key}" %>
  <%= tag.span(classes, class: classes) %>
<% end %>

<h4>Bootstrap 5 syntax (requires adding text-dark for warning, info and light)</h4>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <% classes = "badge bg-#{key}" %>
  <% classes += ' text-dark' if %i[warning info light].include?(key) %>
  <%= tag.span(classes, class: classes) %>
<% end %>

<h4>Bootstrap 5.2 syntax (use this!)</h4>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <% classes = "badge text-bg-#{key}" %>
  <%= tag.span(classes, class: classes) %>
<% end %>

<h3>Badge pills</h1>

<h4>Bootstrap 4 syntax</h4>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <% classes = "badge badge-#{key} badge-pill" %>
  <%= tag.span(classes, class: classes) %>
<% end %>

<h4>Bootstrap 5 syntax (requires adding text-dark for warning, info and light)</h4>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <% classes = "badge bg-#{key} rounded-pill" %>
  <% classes += ' text-dark' if %i[warning info light].include?(key) %>
  <%= tag.span(classes, class: classes) %>
<% end %>

<h4>Bootstrap 5.2 syntax (use this!)</h4>
<% Colours::PALETTE[:bootstrap].each do |key, _value| %>
  <% classes = "badge text-bg-#{key} rounded-pill" %>
  <%= tag.span(classes, class: classes) %>
<% end %>

<h3>Links</h3>

<h4><%= link_to 'header link', '/', class: '' %></h4>

<%= link_to 'a link', '/', class: '' %>

<h3>Forms</h3>

<input type='checkbox' checked> Checkbox<br>
<input type='radio' checked> Radio<br>
<input type="range" name="cowbell" min="0" max="100" value="90" step="10" autocomplete='off'><br>
<progress id="file" max="100" value="70">70%</progress>

<hr>

<div class="form-check">
  <input class="form-check-input" type="checkbox" id="customCheck" name="example1" checked>
  <label class="form-check-label" for="customCheck">Custom checkbox</label>
</div>

<div class="form-check">
  <input class="form-check-input" type="radio" id="customRadio" name="example2" value="customEx" checked>
  <label class="form-check-label" for="customRadio">Custom radio</label>
</div>

<h2>Rounding</h2>

<h3>Bootstrap 4</h3>
<div class='d-flex align-items-start m-2'>
  <div class='bg-grey-light rounded-0 p-2 m-2' style='width:200px'>.rounded-0</div>
  <div class='bg-grey-light rounded-sm p-2 m-2' style='width:200px'>.rounded-sm</div>
  <div class='bg-grey-light rounded p-2 m-2' style='width:200px'>.rounded</div>
  <div class='bg-grey-light rounded-lg p-2 m-2' style='width:200px'>.rounded-lg</div>
</div>

<h3>Extra</h3>
<div class='d-flex align-items-start m-2'>
  <div class='bg-grey-light rounded-xl p-2 m-2' style='width:200px'>.rounded-xl</div>
</div>

<h3>Bootstrap 5 mappings</h3>
<div class='d-flex align-items-start m-2'>
  <div class='bg-grey-light rounded-0 p-2 m-2' style='width:200px'>.rounded-0</div>
  <div class='bg-grey-light rounded-1 p-2 m-2' style='width:200px'>.rounded-1</div>
  <div class='bg-grey-light rounded-2 p-2 m-2' style='width:200px'>.rounded-2</div>
  <div class='bg-grey-light rounded-3 p-2 m-2' style='width:200px'>.rounded-3</div>
</div>

<h2>Colours</h2>

<div>
  <% Colours::PALETTE.each do |key, group| %>
    <% grouped, ungrouped = group.partition { |_, value| value.is_a?(Hash) }.map(&:to_h) %>
    <% grouped.each do |name, shades| %>
      <h3><%= key %>: <%= name %></h3>
      <div class="row g-0">
        <% shades.each do |shade, mapping| %>
          <%= render 'colour', name: "#{name}_#{shade}", map: mapping, hex: Colours.hex(mapping) %>
        <% end %>
      </div>
    <% end %>
    <% if ungrouped.any? %>
      <h3><%= key %></h3>
      <div class="row g-0">
        <% ungrouped.each do |name, value| %>
          <%= render 'colour', name: name, map: value, hex: Colours.hex(value) %>
        <% end %>
      </div>
    <% end %>
  <% end %>
</div>

<div>
  <h2>Default Chart Colours</h2>
  <div class="row g-0">
    <% Colours::DEFAULT_CHART_COLOURS.each do |colour| %>
      <%= render 'colour', hex: colour %>
    <% end %>
  </div>
</div>

<div>
  <h2>Validated Meter Readings Colours</h2>
  <div class="row g-0">
    <% Colours::AMR_COLOURS.each do |colour| %>
      <%= render 'colour', hex: colour %>
    <% end %>
  </div>
</div>

<div>
  <h2>Transport Chart Colours</h2>
  <div class="row g-0">
    <% Colours::TRANSPORT_CHART_COLOURS.each do |colour| %>
      <%= render 'colour', hex: colour %>
    <% end %>
  </div>
</div>
