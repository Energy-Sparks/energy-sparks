<div class="d-flex justify-content-between align-items-center">
  <h1>Issue Tags</h1>
</div>

<%= link_to 'New issue_tag', new_admin_issue_tag_path, class: 'btn btn-primary mb-2' %>

<div id='issue-tags-list'>
  <% if @issue_tags.any? %>
    <table class="table table-sorted">
      <thead>
        <tr>
          <th>Label</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @issue_tags.each do |issue_tag| %>
          <tr>
            <td><%= issue_tag.label %></td>
            <td class="fit nowrap text-end">
              <%= link_to 'View issues with this tag', admin_issues_path, class: 'btn btn-secondary btn-sm edit' %>
              <%= link_to 'Edit', edit_admin_issue_tag_path(issue_tag), class: 'btn btn-secondary btn-sm edit' %>
              <%= link_to 'Delete',
                          admin_issue_tag_path(issue_tag),
                          method: :delete,
                          data: { confirm: 'Are you sure?' },
                          class: 'btn btn-danger btn-sm' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p>No issue tags to display.</p>
  <% end %>
</div>
