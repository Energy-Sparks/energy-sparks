<thead class="thead-light">
  <tr scope="row">
    <th>
      <%= fa_icon(fuel_type_icon(meter_attribute.meter_type)) %> <%= meter_attribute.meter_type %> <%= "(pseduo)" if meter_attribute.pseudo? %>
    </th>
    <th></th>
  </tr>
</thead>
<tbody>
  <tr>
    <td><%= print_meter_attribute(meter_attribute) %></td>
    <td>
      <%= yield %>
      <%= render 'shared/meter_attributes/details_table', meter_attribute: meter_attribute %>
    </td>
  </tr>
  <tbody>
