<%= render 'header', title: "Edit #{@note.note_type.capitalize} for #{@school.name}" do %>
  <%= header_nav_link 'All school notes & issues', admin_school_notes_url(@school) %>
<% end %>

<%= render 'form', school: @school, note: @note, index: false %>
