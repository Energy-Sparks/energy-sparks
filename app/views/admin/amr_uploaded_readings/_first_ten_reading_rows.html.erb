<p>
  The following table shows up to 10 rows from the uploaded file.
</p>
<div class="table-responsive">
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Meter ID</th>
        <th>Mpan Mprn</th>
        <th>Reading Date</th>
        <th colspan="48">Readings</th>
      </tr>
    </thead>
    <tbody>
      <% valid_reading_data.each do |row| %>
        <tr>
          <td><%= row["meter_id"] %></td>
          <td><%= row["mpan_mprn"] %></td>
          <td><%= row["reading_date"] %></td>
          <% row["readings"].each do |reading| %>
            <td><%= reading %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
