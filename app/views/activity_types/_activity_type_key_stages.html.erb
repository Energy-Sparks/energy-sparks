<% activity_type.key_stages.order(:name).each do |key_stage| %>
  <span class="badge badge-info" data-toggle="tooltip" data-placement="bottom" title="Key Stages"><%= key_stage.name %></span>
<% end %>
