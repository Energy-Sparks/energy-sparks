<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <%= render 'shared/head' %>
  </head>
  <body>
    <div class="fixed-top">
      <nav class="navbar <%= navbar_expand_class %> navbar-custom">
        <div class="container">
          <%= navbar_image_link %>
          <%= on_test_link %>
          <%= locale_switcher_buttons %>
        </div>
      </nav>
    </div>
    <div class="application container">
      <div>
        <h1>Energy Sparks is currently down for maintenance</h1>

        <p>
          We're sorry but Energy Sparks is currently available while we carry
          out some essential maintenance.
        </p>

        <p>
          Our team is currently working on updating the site and we expect
          the service to be available again in a couple of hours.
        </p>

      </div>
    </div>
    <!-- Footer -->
    <footer>
      <div class="footer-above">
        <div class="container">
          <div class="row">
            <div class="col-4">
              <p class="small">
                <%= t('footer.registered_charity_notice') %>.
              </p>
              <div class="row">
                <div class="col-2">
                  <%= fab_icon('creative-commons fa-2x') %>
                </div>
                <div class="col-10">
                  <p class="small">
                    <%= t('footer.cc_licence_notice_html') %>.
                  </p>
                </div>
               </div>
            </div>
            <div class="col-5">
            </div>
            <div class="col-3">
              <h6><%= t('footer.follow_energy_sparks') %></h6>
              <ul class="list-inline">
                <li class="list-inline-item p-2">
                  <a href="//twitter.com/EnergySparks" class=""><%= fab_icon('twitter fa-2x') %></a>
                </li>
                <li class="list-inline-item p-2">
                  <a href="//www.facebook.com/EnergySparksUK/" class=""><%= fab_icon('facebook-square fa-2x') %></a>
                </li>
                <li class="list-inline-item p-2">
                  <a href="//github.com/BathHacked/energy-sparks" class=""><%= fab_icon('github fa-2x') %></a>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>

    </footer>

    <%= render 'shared/google_analytics', analytics_code: @analytics_code if Rails.env.production? && !@analytics_code.blank? %>
  </body>
</html>
