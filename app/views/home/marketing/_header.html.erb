<header>
  <div class="row masthead">
    <div class="col-md-7">
    </div>
    <div class="col-md-5 masthead-content">
      <div class="row pt-5">
        <div class="col-md-12">
          <h1 class="display-4"><strong><%= title %></strong></h1>
      </div>
      </div>
      <div class="row">
        <div class="col-md-12 pr-5">
          <h1><%= t('home.sub_title') %></h1>
        </div>
      </div>
      <div class="row">
        <div class="col-md-12 pt-5 text-center cta">
          <% if local_assigns[:school] %>
            <%= link_to t('marketing.book_a_demo'), training_path, class: 'btn bg-positive' %>
          <% else %>
            <%= render 'home/marketing/book_demo_embed', label: t('marketing.book_a_demo') %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</header>
