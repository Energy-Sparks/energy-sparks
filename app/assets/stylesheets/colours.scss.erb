// Core colours for use across the application

// The hex values for colours can be found in initializers/colours.rb

// The following creates a SASS variable for colours grouped in the Colours::PALETTE hash
// e.g.: Colours.sass_variables :palette
// creates variables for the :palette group, such as:
// $yellow-pale: #fdefc8;
// $blue-very-dark: #334375;

// Examples of variables bought in from fuel, polarity and comparison groups:
// $electric-dark: #007EFF;
// $positive-dark: #10bca2;
// $exemplar-school: #10bca2;
<%= Colours.sass_variables :palette %>
<%= Colours.sass_variables :fuel %>
<%= Colours.sass_variables :polarity %>
<%= Colours.sass_variables :comparison %>
<%= Colours.sass_variables :bootstrap %>
<%= Colours.sass_variables :theme %>

// Create SASS maps for each of these keys found in Colours::PALETTE hash
<%= Colours.sass_map :fuel %>
<%= Colours.sass_map :polarity %>
<%= Colours.sass_map :comparison %>

// Get a colour from a generated map
// not used yet so need to check if working :-)
@function colour-get($hash, $name, $shade: false) {
  @if $shade {
    @return map-get(map-get($hash, $name), $shade);
  } @else {
    @return map-get($hash, $name);
  }
}

// Old / current colour mappings ###
// These are *not* to be used for new features as we're moving away
// from them to the colours above

// Yellows
$new-yellow: <%= Colours::NEW_YELLOW %>; // not used in the site but same colour is used in mailers & chart_data_values.rb

// Oranges
$dark-orange: <%= Colours::DARK_ORANGE %>;
$light-orange: <%= Colours::LIGHT_ORANGE %>;

// Greens
$green: <%= Colours::GREEN %>;

// PURPLES / PINKS
$mid-purple: <%= Colours::MID_PURPLE %>;

// Turquoise
$turquoise: <%= Colours::TURQUOISE %>;
$light-turquoise: <%= Colours::LIGHT_TURQUOISE %>;

// Shades of grey
$dark: <%= Colours::DARK %>;
$dark-grey: <%= Colours::DARK_GREY %>;
$silver: <%= Colours::SILVER %>;
$grey: <%= Colours::GREY %>;
$light-grey: <%= Colours::LIGHT_GREY %>;
$lighter-grey: <%= Colours::LIGHTER_GREY %>;

// Shortcuts
$text: $dark;
