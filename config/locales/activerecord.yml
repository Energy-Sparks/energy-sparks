---
en:
  activerecord:
    attributes:
      activity_type:
        description: Description
        description_cy: Description
        description_en: Description
        download_links: Download links
        download_links_cy: Download links
        download_links_en: Download links
        name: Name
        name_cy: Name
        name_en: Name
        school_specific_description: School specific description
        school_specific_description_cy: School specific description
        school_specific_description_en: School specific description
        summary: Summary
        summary_cy: Summary
        summary_en: Summary
      intervention_type:
        description: Description
        description_cy: Description
        description_en: Description
        download_links: Download links
        download_links_cy: Download links
        download_links_en: Download links
        name: Title
        name_cy: Title
        name_en: Title
        summary: Summary
        summary_cy: Summary
        summary_en: Summary
    errors:
      messages:
        record_invalid: 'Validation failed: %{errors}'
        restrict_dependent_destroy:
          has_many: Cannot delete record because dependent %{record} exist
          has_one: Cannot delete record because a dependent %{record} exists
  errors:
    attributes:
      temperature_recording:
        centigrade: Temperature
    format: "%{attribute} %{message}"
    messages:
      accepted: must be accepted
      blank: can't be blank
      confirmation: doesn't match %{attribute}
      empty: can't be empty
      equal_to: must be equal to %{count}
      even: must be even
      exclusion: is reserved
      greater_than: must be greater than %{count}
      greater_than_or_equal_to: must be greater than or equal to %{count}
      inclusion: is not included in the list
      invalid: is invalid
      less_than: must be less than %{count}
      less_than_or_equal_to: must be less than or equal to %{count}
      model_invalid: 'Validation failed: %{errors}'
      not_a_number: is not a number
      not_an_integer: must be an integer
      odd: must be odd
      other_than: must be other than %{count}
      present: must be blank
      required: must exist
      taken: has already been taken
      too_long:
        one: is too long (maximum is 1 character)
        other: is too long (maximum is %{count} characters)
      too_short:
        one: is too short (minimum is 1 character)
        other: is too short (minimum is %{count} characters)
      wrong_length:
        one: is the wrong length (should be 1 character)
        other: is the wrong length (should be %{count} characters)
    template:
      body: 'There were problems with the following fields:'
      header:
        one: 1 error prohibited this %{model} from being saved
        other: "%{count} errors prohibited this %{model} from being saved"
